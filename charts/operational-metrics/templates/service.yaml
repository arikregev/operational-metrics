apiVersion: v1
kind: Service
metadata:
  name: {{ include "operational-metrics.fullname" . }}
  labels:
    {{- include "operational-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operational-metrics.selectorLabels" . | nindent 4 }}
